{
  "name": "bloom-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --binding BLOOM_KV=... --kv BLOOM_KV",
    "typecheck": "tsc --noEmit",
    "build": "echo 'No build step needed — Pages compiles TypeScript functions automatically'"
  },
  "devDependencies": {
    "wrangler": "^3.40.0",
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3"
  }
}
